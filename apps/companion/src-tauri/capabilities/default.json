{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the companion window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
